
#include "Vehicle.h"

// Vehicle::F Vehicle::turn_radius = 10; // cm

// Vehicle::M Vehicle::step2dist = M::unit() * Motor::encoder_resolution * Motor::wheel_radius;
// Vehicle::M Vehicle::dist2dso = M(0.5, 0.5, 1 / turn_radius, -1 / turn_radius);

// Vehicle::M Vehicle::step2dso = step2dist * dist2dso;
// Vehicle::M Vehicle::dso2step = ~step2dso;

// Vehicle::V Vehicle::XY = Vehicle::V::null();
// Vehicle::F Vehicle::O = 0;
//
// Vehicle::M Vehicle::orientation = Vehicle::M(-1, 0, 0, 1);
